[package]
name = "im920_rs"
version = "0.1.0"
edition = "2021"

[build-dependencies]
cbindgen = "0.27.0"

[dependencies]
srobo_base = { features=["alloc"], version = "v1.0.1" }

[features]
default = []
std = []